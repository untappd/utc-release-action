{
  "name": "action-release",
  "version": "1.0.0",
  "description": "Tag and create release from master to production using date as tag. Creates a PR to move the changes to production.",
  "main": "lib/main.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/github": "^4.0.0"
  }
}
